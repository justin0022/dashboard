import createSVG from '../d3/createSVG'
import compose from '../../util/compose'

const createComposedGraph = ({ charts, data, x, y }) => {
  const svg = createSVG()
}

export default createComposedGraph
